[project]
name = "folio-scripts"
version = "0.1.0"
description = "Python scripts for portfolio automation"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "httpx>=0.27",
  "rich>=13.0",
]
